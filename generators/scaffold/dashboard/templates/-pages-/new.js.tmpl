import React from 'react';
import { useNavigate } from 'react-router-dom';
import { useMutation } from '@apollo/react-hooks';
import { gql } from 'apollo-boost';
import { {{.opts.Name}}Form } from 'components/src/{{.opts.Name.Pluralize.ToLower}}';

const {{.opts.Name}}NewPage = (props) => {
  const navigate = useNavigate();
  const [create{{.opts.Name}}, { data }] = useMutation(CREATE_{{.opts.Name.ToUpper}});

  function handleSubmit(values, { setSubmitting }) {
    create{{.opts.Name}}({variables: { input: values }}).then((resp) => {
      setSubmitting(false)
      navigate(`/{{.opts.Name.Pluralize.ToLower}}/${resp.data.create{{.opts.Name}}.id}`)
    }).catch(err => {
      console.warn(err);
      setSubmitting(false);
      alert('Create failed')
    })
  }

  return (
    <div>
      <h2>New {{.opts.Name}}</h2>
      <{{.opts.Name}}Form
        initialValues={{"{{"}}
        {{"}}"}}
        onSubmit={handleSubmit}
      />
    </div>
  );
};

const CREATE_{{.opts.Name.ToUpper}} = gql`
  mutation Create{{.opts.Name}}($input: Create{{.opts.Name}}Input!) {
    create{{.opts.Name}}(input: $input) {
      id
    }
  }
`;

export default {{.opts.Name}}NewPage;

import React from 'react';
import { NavLink } from 'react-router-dom';

const Sidebar = () => {
  return (
    <div className="sidebar">
      <div className="sidebar-logo">{{ .opts.Name }}</div>
      <NavLink className="sidebar-item" to="/" end>
        Home
      </NavLink>
      <NavLink className="sidebar-item" to="/graphiql">
        GraphiQL
      </NavLink>
    </div>
  );
};

export default Sidebar;
